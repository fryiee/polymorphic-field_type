# Output

This field type returns the related entry by default.

**Examples:**

```
// Twig usage
{{ entry.example.id }} or {{ entry.example.name }}

// API usage
$entry->example->id; or $entry->example->name;
```
